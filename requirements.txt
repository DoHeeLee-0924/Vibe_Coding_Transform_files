# 로컬 파일 변환기 - 파이썬 의존성
# 설치:  python -m pip install -r requirements.txt

Flask==3.1.3
Pillow==12.3.0
PyMuPDF==1.28.0

# ---------------------------------------------------------------------------
# 외부 프로그램 (pip으로 설치되지 않음 - 별도 설치 필요)
#
# [영상 변환]  ffmpeg  ... MP4 / WEBM / GIF 변환에 필요
#   Windows(winget):  winget install --id Gyan.FFmpeg -e
#   설치 후 run.bat(서버)을 다시 실행하면 자동으로 인식됩니다.
#
# [OCR - 아직 미구현]  Tesseract  ... 스캔 PDF/이미지의 글자 인식(예정)
# ---------------------------------------------------------------------------
